# Disable RDB snapshots to avoid disk I/O during benchmarking.
save ""

# Disable AOF persistence to maximize throughput for benchmarks.
appendonly no

# Listen on all interfaces so Spring Boot on host can connect via localhost.
bind 0.0.0.0
